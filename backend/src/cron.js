// const cron = require("node-cron");
// const { fetchAndStoreRates } = require("../controllers/currency_rates");

// // Every 45 minutes
// cron.schedule("*/45 * * * *", async () => {
//   await fetchAndStoreRates();
// });

// // Run once on server start
// fetchAndStoreRates();
